export type Place = {
  pk: number;
  name: string;
  coordinate: {
    x: number;
    y: number;
  };
};

export type PlaceResponse = {
  pk: number;
  name: string;
  coordinate: {
    x: number;
    y: number;
  };
};
