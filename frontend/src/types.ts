export type Place = {
  name: string;
  coordinate: {
    latitude: number;
    longitude: number;
  };
};

export type PlaceResponse = {
  name: string;
  coordinate: {
    latitude: number;
    longitude: number;
  };
};
